# AWS Amplify Lambda Function URLs
# These will be generated after deploying the backend
# Copy this file to .env and fill in the values after deployment

# Fast CRUD operations (director appointments, tasks, service requests, etc.)
VITE_API_HANDLER_URL=https://your-api-handler-url.lambda-url.region.on.aws/

# Heavy operations (PDF generation, form processing, OCR, etc.)
VITE_HEAVY_OPERATIONS_URL=https://your-heavy-operations-url.lambda-url.region.on.aws/
